import type { Converter, Mapping, PrimitiveConstructorExtended, ValueSelector } from '../types';
/**
 *
 * Apply converters if available to mapInitialize on createMap
 *
 * @param mapping
 * @param typeConverters
 */
export declare function applyTypeConverters(mapping: Mapping, typeConverters: Map<PrimitiveConstructorExtended, Map<PrimitiveConstructorExtended, ValueSelector | Converter>>): void;
