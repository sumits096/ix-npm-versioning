"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isSamePath = exports.uniquePaths = void 0;
function uniquePaths(paths) {
    const result = [];
    for (let i = 0, pathsLen = paths.length; i < pathsLen; i++) {
        const value = paths[i];
        if (!result.some((item) => isSamePath(item, value))) {
            result.push(value);
        }
    }
    return result;
}
exports.uniquePaths = uniquePaths;
function isSamePath(target, value) {
    if (target.length !== value.length) {
        return false;
    }
    for (let i = 0, targetLen = target.length; i < targetLen; i++) {
        if (target[i] !== value[i]) {
            return false;
        }
    }
    return true;
}
exports.isSamePath = isSamePath;
//# sourceMappingURL=unique.util.js.map