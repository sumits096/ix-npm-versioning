import type { Dictionary, ErrorHandler } from '../types';
/**
 * Depends on implementation of plugin.initializeMapping
 */
export declare function assertUnmappedProperties<TDestination extends Dictionary<TDestination> = any>(destination: TDestination, configuredKeys: string[], sourceKey: unknown, destinationKey: unknown, errorHandler: ErrorHandler): void;
