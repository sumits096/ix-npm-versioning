"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSourcePropertyPath = void 0;
const utils_1 = require("../utils");
function getSourcePropertyPath(path, namingConventions) {
    if (!(0, utils_1.isDefined)(namingConventions)) {
        return path;
    }
    const [sourceNamingConvention, destinationNamingConvention] = namingConventions;
    const keyParts = path
        .map((s) => s.split(destinationNamingConvention.splittingExpression).filter(Boolean))
        .filter((p) => p.length > 0);
    return !keyParts.length
        ? path
        : keyParts.map((p) => sourceNamingConvention.transformPropertyName(p));
}
exports.getSourcePropertyPath = getSourcePropertyPath;
//# sourceMappingURL=get-source-property-path.util.js.map