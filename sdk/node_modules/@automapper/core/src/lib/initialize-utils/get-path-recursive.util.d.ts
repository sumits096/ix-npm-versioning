/**
 * Loop through an object and recursively get paths of each property
 *
 * @param node
 * @param prefix
 * @param prev
 */
export declare function getPathRecursive(node: Record<string, unknown>, prefix?: string[], prev?: string[][]): string[][] | undefined;
