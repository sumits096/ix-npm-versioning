import { ServiceInterface } from '.';
import { BaseResponse } from '../../entity';

/**
 * Defines methods defination for customer to interact with customer repository.
 *
 * @interface CustomerServiceInterface
 */
export interface CustomerServiceInterface extends ServiceInterface<BaseResponse> {}
